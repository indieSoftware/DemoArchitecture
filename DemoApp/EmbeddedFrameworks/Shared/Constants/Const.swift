public enum Const {
	// Specific constants will be defined in their extensions.
}

extension Const {
	/// The namespace for all localized strings in the project.
	public enum Text {
		// Has to be added via extensions.
	}
}
