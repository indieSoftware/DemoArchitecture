import UIKit

extension Const {
	/// All font types with a specific size used in the app.
	public enum Font {
		/// A big font, i.e. for the splash screen's title.
		public static let splashTitle = UIFont.boldSystemFont(ofSize: 30)
	}
}
